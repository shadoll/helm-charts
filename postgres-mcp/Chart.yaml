apiVersion: v2
name: postgres-mcp
description: Multi-database Postgres MCP server Helm chart (FreePeak/db-mcp-server)
type: application
version: 0.1.1
appVersion: "1.9.0"
annotations:
  version-source: github-release:FreePeak/db-mcp-server
  version-pattern: "s|^v||"
