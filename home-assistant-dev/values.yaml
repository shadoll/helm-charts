home-assistant:
  image:
    # Must match Chart.yaml appVersion — subchart can't access parent's appVersion
    tag: "2026.6.0.dev202605020315"

  hostNetwork: false

  http:
    enabled: true

  homekit:
    enabled: false

  persistence:
    config:
      type: pvc
      size: 5Gi
      storageClass: local-path
    media:
      enabled: false

  resources:
    requests:
      cpu: "100m"
      memory: "512Mi"
    limits:
      cpu: "2"
      memory: "2Gi"
